{
  "$schema": "https://json-schema.org/draft/2020-12/schema#",
  "description": "Explicit pinned SHAs for the cross-repository integrity workflow (.github/workflows/cross-repo-integrity.yml). Update deliberately, in the same PR as the change that requires a new pin, never silently.",
  "repos": {
    "data-connect": {
      "owner": "PDP-Connect",
      "repo": "data-connect",
      "sha": "ed0dbaca5a3845200e9e0fc230900620e5f69b07",
      "branch": "main",
      "note": "Merge commit of data-connect#116 into PDP-Connect/data-connect main (merged 2026-09-14T14:49:13Z), captured from main's head and confirmed an ancestor of main via `git merge-base --is-ancestor` — never a PR head. This pin now backs THREE drift jobs, none of which involve a packed tarball: (a) collector-definitions, which compares this repo's connector registry against data-connect's committed collector-definitions.generated.ts using packages/local-collector/scripts/generate-collector-definitions-snapshot.ts; (b) connector-sources, which byte-compares the six connector directories data-connect vendors from this repo (claude_code, codex, google_takeout, imessage, apple_photos, google_messages); and (e) collector-packaging-manifest, which compares the registry against packages/local-collector/tsconfig.build.json, the compile list. Those three are source-level comparisons against files committed in data-connect, so they still need an explicit, deliberately-moved SHA — a mutable branch ref would let an upstream change flip them without review. What this pin NO LONGER backs: drift job (c) tarball-digests, retired along with the hand-vendored pdpp-collector-runtime-0.0.1.tgz / pdpp-connector-protocol-0.0.1.tgz. Both packages now install from the npm registry at an exact pinned version, so their provenance is the registry's immutable published artifact plus the lockfile's integrity hash — not a digest list maintained by hand in this repo. The companion `data-connect-1-0-0` pin was deleted in the same change: it existed ONLY to give job (c)'s 1.0.0 leg a second, later data-connect checkout to repack pdpp's 1.0.0 tarballs from, and with job (c) gone it had no remaining reader. packages/collector-runtime and packages/connector-protocol are likewise dropped from the pin-freshness path list for this repo, because npm version resolution — not a path diff against this SHA — is now what governs those two packages."
    },
    "pdpp": {
      "owner": "PDP-Connect",
      "repo": "pdpp",
      "sha": "7fc529a6467a2895db4e7f54f7877c977c475680",
      "branch": "main",
      "note": "Head of PDP-Connect/pdpp main on 2026-09-14, the merge of pdpp#361 'chore(vendor): drop the unused collector-runtime 0.0.1 archive', captured from main's head via `git ls-remote origin refs/heads/main` and confirmed an ancestor of main via `git merge-base --is-ancestor` — never a PR head, for the reason the data-connect entry's superseded-pin history above records. Moved from 55c00d346 to unblock drift job (c) tarball-digests, whose pdpp leg has failed on every pull request in this repo since data-connect#106 landed. At 55c00d346, pdpp's vendor/SHA256SUMS recorded pdpp-collector-runtime-0.0.1.tgz at e274fb459cce011f3c290ab92a0d83252fa91af671d33e37e3809edea06fafbf, the digest of a tarball packed BEFORE #106 rewrote collector-runtime's durable outbox; job (c) repacks every recorded entry fresh from the `data-connect` pin above, so that entry could no longer reconcile and the leg reported 'FAIL: pdpp — pdpp-collector-runtime-0.0.1.tgz content manifest mismatch'. The fix belonged in pdpp, and pdpp#361 made it: the 0.0.1 archive was dead weight there — nothing in pdpp consumed it — so #361 deleted both the tarball and its SHA256SUMS line rather than re-vendoring bytes pdpp does not use. At this SHA pdpp's vendor/SHA256SUMS records only the two 1.0.0 release-boundary entries, which job (c) reproduces from the separate `data-connect-1-0-0` pin and which are unaffected by #106. Verified by running the job's own check-tarball-digest-drift.sh against clean clones at all four pinned SHAs: every leg passes, including this repo's own vendor/SHA256SUMS. Job (d) reference-contract compares identical bytes before and after this move — `git log 55c00d346..7fc529a64 -- packages/reference-contract` is empty, and the other eleven commits in that range touch only site content and docs — so the copied reference-contract modules are unchanged and the pin-freshness pdpp leg, which diffs that same path against pdpp's tracked ref, passes trivially with the pin now exactly at main's head. This is a pin-reference update only: no file in this repository changes besides this entry."
    }
  }
}
