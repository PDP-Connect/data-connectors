{
  "$schema": "https://json-schema.org/draft/2020-12/schema#",
  "description": "Explicit pinned SHAs for the cross-repository integrity workflow (.github/workflows/cross-repo-integrity.yml). Update deliberately, in the same PR as the change that requires a new pin, never silently. NOTE: drift job (c) 'vendored tarball digests', referenced in the notes below, was retired once @pdpp/collector-runtime and @pdpp/connector-protocol started publishing to npm from data-connect (PDP-Connect/data-connect#32) instead of being vendored as committed tarballs here — the notes are left as-written since they're a record of past pin decisions, not live job documentation.",
  "repos": {
    "data-connect": {
      "owner": "PDP-Connect",
      "repo": "data-connect",
      "sha": "9155e57ae47ab145214eb10551ed2c2185d7098a",
      "branch": "main",
      "note": "Head of PDP-Connect/data-connect main as of 2026-08-20, verified via `gh api repos/PDP-Connect/data-connect/branches/main` at pin time. This is the merge commit of PR #30, which ports three hunks pdpp's own rebase of fix/preservation-fixes-0819 had dropped (bare-specifier package validation, iMessage fixture date fix, connector-spawn tsx-resolution hardening). Supersedes the prior 9ef24d46 pin (PR #29, consumer-pin-required activation): only packages/collector-runtime changed between the two pins (see this repo's vendor/SHA256SUMS refresh in the same PR that updated this field). Source of truth for drift jobs (a) collector-definitions snapshot, (b) vendored connector sources, (c) tarball digests."
    },
    "pdpp": {
      "owner": "PDP-Connect",
      "repo": "pdpp",
      "sha": "5eacd82e2f9822b555bda869429b842be66b1ff7",
      "branch": "main",
      "note": "Head of PDP-Connect/pdpp main as of 2026-08-20, verified via `gh api repos/PDP-Connect/pdpp/branches/main` at pin time. This is the merge commit of PR #171, which refreshed pdpp's own vendor/pdpp-collector-runtime-0.0.1.tgz to match data-connect's new pin above (both moves are part of the same coordinated data-connect PR #30 repin). Required here: drift job (c) (vendored tarball digests) checks pdpp's vendor/SHA256SUMS against a fresh repack from the data-connect pin too, and pdpp's tarball at the PRIOR a0924e9a pin predates PR #171, so it would fail that comparison without this move. packages/reference-contract (what this pin otherwise backs, for drift job (d)) did not change in PR #171. Supersedes the prior a0924e9a pin (PR #170, consumer-pin-required activation)."
    }
  }
}
